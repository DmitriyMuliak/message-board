/** Public API of the `feed` view (FSD's `pages` layer — renamed to avoid a
 * collision with Next's own `pages/` convention). */

export { FeedView } from './ui/FeedView';
export { FeedSkeleton } from './ui/FeedSkeleton';
export { FeedError } from './ui/FeedError';
